<?php
/**
 * Template Name: Full-width
 *
 * Description: Use this page template for full-width display.
 *
 */

// Full-width functionality is handled by testing against the name of the page template; the actual code is otherwise the same as `../single.php`
get_template_part( 'single' );
